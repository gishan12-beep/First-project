# Simple Python Addition Script

This is a simple Python script that defines a function to add two numbers together. It includes a brief example demonstrating the use of the `add_two_numbers` function.